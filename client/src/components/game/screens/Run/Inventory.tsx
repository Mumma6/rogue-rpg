import React from 'react'

const Inventory = ({ toogle, hero }: any) => {
  return (
    <div>
      ändra inventor för en hero
      . lägg till drag and drop?
      Även vissa stats osv här?
    </div>
  )
}

export default Inventory
